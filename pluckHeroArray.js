//TODO use splice function to remove specific arrays from the heroes array, then pass to determineBestHero

function pluckHeroArray(heroArray)
{
  heroPass = heroArray;
if (anasrc !== "ana.png")
{
  heroMod = heroPass.splice(anaSlice);
  heroPass = heroArray;
}
if (bastionsrc !== "bastion.png")
{
  heroesMod = heroPass.splice(bastionSlice);
  heroPass = heroArray;
}
if (dvasrc !== "dva.src")
{
  heroesMod = heroPass.splice(dvaSlice);
  heroPass = heroArray;
}
if (genjisrc !== "genji.src")
{
  heroesMod = heroPass.splice(genjiSlice);
  heroPass = heroArray;
}
if (hanzosrc !== "hanzo.src")
{
  heroesMod = heroPass.splice(hanzoSlice);
  heroPass = heroArray;
}
if (junkratsrc !== "junrat.src")
{
  heroesMod = heroPass.splice(junkratSlice);
  heroPass = heroArray;
}
if (luciosrc !== "lucio.png")
{
  heroesMod = heroPass.splice(lucioSlice);
  heroPass = heroArray;
}
if (mccreesrc !== "mccree.png")
{
  heroesMod = heroPass.splice(mccreeSlice);
  heroPass = heroArray;
}
if (meisrc !== "mei.png")
{
  heroesMod = heroPass.splice(meiSlice);
  heroPass = heroArray;
}
if (mercysrc !== "mercy.png")
{
  heroesMod = heroPass.splice(mercySlice);
  heroPass = heroArray;
}
if (pharahsrc !== "pharah.png")
{
  heroesMod = heroPass.splice(pharahSlice);
  heroPass = heroArray;
}
if (reapersrc !== "reaper.png")
{
  heroesMod = heroPass.splice(reaperSlice);
  heroPass = heroArray;
}
if (reinsrc !== "rein.png")
{
  heroesMod = heroPass.splice(reinSlice);
  heroPass = heroArray;
}
if (roadhogsrc !== "roadhog.png")
{
  heroesMod = heroPass.splice(roadhogSlice);
  heroPass = heroArray;
}
if (soldiersrc !== "soldier.png")
{
  heroesMod = heroPass.splice(soldierSlice);
  heroPass = heroArray;
}
if (symmetrasrc !== "symmetra.png")
{
  heroesMod = heroPass.splice(symmetraSlice);
  heroPass = heroArray;
}
if (torbjornsrc !== "torbjorn.png")
{
  heroesMod = heroPass.splice(torbjornSlice);
  heroPass = heroArray;
}
if (tracersrc !== "tracer.png")
{
  heroesMod = heroPass.splice(tracerSlice);
  heroPass = heroArray;
}
if (widowsrc !== "widow.png")
{
  heroesMod = heroPass.splice(widowSlice);
  heroPass = heroArray;
}
if (winstonsrc !== "winston.png")
{
  heroesMod = heroPass.splice(winstonSlice);
  heroPass = heroArray;
}
if (zaryasrc !== "zarya.png")
{
  heroesMod = heroPass.splice(zaryaSlice);
  heroPass = heroArray;
}
if (zenyattasrc !== "zenyatta.png")
{
  heroesMod = heroPass.splice(zenyattaSlice);
  heroPass = heroArray;
}

return heroesMod;
}
